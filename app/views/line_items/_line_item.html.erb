
<%# <% line_item.destroy if line_item.quantity == 0 %>
<% if line_item == @current_item  %>
  <tr id="current_item">
<% else %>
  <tr>
<% end %>
<% unless @is_email  %>
    <td><%= button_to('+', line_items_path(:product_id => line_item.product),
                      :method => :create, :id => 'plus', :remote => true ) %></td>
    <td><%= button_to('-', line_item_path(line_item), :method => :delete, :remote => true) %></td>
<% end  %>
    <td><%=line_item.quantity%>&times;</td>
    <td><%= line_item.product.title %></td>
    <td class="item_price"><%= number_to_currency(line_item.total_price) %></td>
</tr>

